import XCTest
@testable import PrincipalAppiOSSPM

final class PrincipalAppiOSSPMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PrincipalAppiOSSPM().text, "Hello, World!")
    }
}
